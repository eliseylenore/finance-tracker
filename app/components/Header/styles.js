import {StyleSheet} from 'react-native';

export default (styles = StyleSheet.create({
  headerContainer: {
    marginTop: 40,
  },
  headerText: {
    color: 'white',
    fontSize: 22,
    fontWeight: '500',
  },
}));
