import {StyleSheet} from 'react-native';

export default (styles = StyleSheet.create({
  input: {
    paddingTop: 10,
    paddingRight: 15,
    fontSize: 34,
    color: 'white',
    fontWeight: '500',
  },
}));
